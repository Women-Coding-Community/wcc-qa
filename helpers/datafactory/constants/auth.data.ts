/** Authentication test data shared across API specs. */

/** Syntactically valid but unknown bearer token, used to assert rejected sessions. */
export const INVALID_TOKEN = "invalid-token-0000000000000000";

/** Password that matches no seeded account, used to assert rejected logins. */
export const WRONG_PASSWORD = "wrong-password-0000";
